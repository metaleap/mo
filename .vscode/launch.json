{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run",
            "type": "gdb",
            "request": "launch",
            "target": "mo_mapgen",
            "cwd": "${fileWorkspaceFolder}/bin",
            "valuesFormatting": "parseText",
            "preLaunchTask": "build_devlocal"
        }
    ]
}
